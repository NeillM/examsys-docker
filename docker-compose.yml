version: '2'
services:
  web:
    depends_on:
      - db
    image: drjosephbaxter/rogo
    volumes:
     - "${ROGO_DOCKER_WWWROOT}:/var/www"
  mail:
    image: dockage/mailcatcher
    ports:
      - "1080:1080"
