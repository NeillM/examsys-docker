version: '2'
services:
  db:
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    image: mysql:${ROGO_DOCKER_MYSQLVERSION}
    environment:
      MYSQL_ROOT_PASSWORD: ${ROGO_DOCKER_MYSQLROOT}
      TZ: ${ROGO_DOCKER_MYSQLTZ}
